#!/usr/bin/env python3
"""
kiln — meta-build tool entry point.
Run from anywhere inside a build tree (forge.toml must be findable above cwd).
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
from kiln.__main__ import main

sys.exit(main())
