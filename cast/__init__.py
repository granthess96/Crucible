"""
Cast — Image generation and projection tool for Crucible.

Consumes component artifacts from Kiln's build cache and generates
deployable images (squashfs, OCI, tar) using FileSpec role annotations.
"""
__version__ = "0.1.0"
